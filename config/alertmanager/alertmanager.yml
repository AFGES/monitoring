route:
  receiver: blackhole

  # Example: route critical alerts to a real receiver
  # routes:
  #   - match:
  #       severity: critical
  #     receiver: webhook-critical

receivers:
  - name: blackhole
  # - name: webhook-critical
  #   webhook_configs:
  #     - url: "http://your-webhook-url"
